{
  "name": "cosmos",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r index.html Cosmos.dc.html support.js schema.md README.md dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
